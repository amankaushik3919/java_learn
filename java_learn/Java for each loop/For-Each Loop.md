Used for loop through elements in an array.

```java
for (type variableName : arrayName) {
	// code block to be executed
}
```

# Example
```java
String[] cars = {"Volvo", "BMW", "Ford", "Mazda"};
for (String i : cars) {
	System.out.println(i);
}
```


